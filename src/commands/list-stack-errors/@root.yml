description: |
  Use the `describe-stack-events` AWS cli command, to list 
  stack events containing errors in a table.

parameters:
  stack-name:
    type: string
    description: |
      The CloudFormation stack name as identified inside AWS.